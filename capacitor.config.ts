import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'washme-customer-app-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
